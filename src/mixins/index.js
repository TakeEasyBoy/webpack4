/**
 * 债权模块踢出的公共部分,相同的api有点多
 */
export const claims = {
  data() {
    return {
      // 用于是否显示左侧的tab信息
      ass_type: false,
      guarant_type: false,
      contract: false
    }
  },
  methods: {
  }
}
